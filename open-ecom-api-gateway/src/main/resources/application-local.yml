server:
  port: 8080

spring:
  application:
    name: open-ecom-api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: orders-service
              uri: http://localhost:8081
              predicates:
                - Path=/order/**
